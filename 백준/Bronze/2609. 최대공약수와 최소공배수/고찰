최대공약수, 최소공배수를 구하는 알고리즘은 유클리드 호제법을 사용하면 편하다.

두 수 a,b(a>b)에 대해서 최대공약수는 a%b와 b의 최대공약수와 같다.
a=b*q+r를, r=a-b*q로 바꿔서 나타내보자. 이 식에서 a와 b의 공약수 d는 r을 나눌 수 있다. 즉 d는 r의 약수이다.
만약 b%r이 0의 값을 가진다면 r은 a,b의 공약수이며, 0이외의 값을 가진다면 a,b를 b,r로 바꾸어 다시 진행하는 것을 반복한다.

이 알고리즘을 코드로 구현하면 다음과 같다.
import sys
a, b = map(int, sys.stdin.readline().split())

x, y = a, b
while y:
    x, y = y, x % y
g = x

print(g)           # 최대공약수
print(a * b // g)  # 최소공배수
